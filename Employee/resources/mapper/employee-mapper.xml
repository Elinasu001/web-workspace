<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="employeeMapper">

	<insert id="insertEmployee" parameterType="com.kh.em.model.vo.Employee">
	
	   INSERT 
	     INTO 
	          EMPLOYEE 
	          (
	          EMP_ID
	        , EMP_NAME
	       	, EMP_NO
	        , EMAIL
	        , PHONE
	        , DEPT_CODE
	        , JOB_CODE
	        , SAL_LEVEL
	        , SALARY
	        , HIRE_DATE
	          )
	  VALUES 
	         (
	         SEQ_EMP_ID.NEXTVAL
	       , #{empName}
	       , #{empNo}
	       , #{email}
	       , #{phone}
	       , #{deptCode}
	       , #{jobCode}
	       , #{salLevel}
	       , #{salary}
	       , SYSDATE
	         )
	</insert>
	
	<select id="findAll" resultType="com.kh.em.model.vo.Employee">
		SELECT
			   EMP_ID 		empId
			 , EMP_NAME 	empName
			 , EMP_NO 		empNo
			 , EMAIL 		email
			 , PHONE		phone
			 , DEPT_CODE 	deptCode
			 , JOB_CODE 	jobCode
			 , SAL_LEVEL 	salLevel
			 , SALARY		salary
			 , BONUS		bonus
			 , MANAGER_ID 	managerId
			 , HIRE_DATE	hireDate
			 , ENT_DATE 	entDate
			 , ENT_YN 		entYn
		  FROM
		       EMPLOYEE
		 ORDER
		    BY
		       EMP_ID DESC 
	</select>
	
	
		      
</mapper>
